[pytest]
# pick up tests under tests/
testpaths = tests

# (optional) your naming conventions
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# enable pytest-asyncio’s own marker
asyncio_mode = auto

# register the marker so the warning goes away
markers =
    asyncio: mark test as asyncio-powered